{#
  wrapper for /admin/config
  parts the cards out into 2 columns
  this template is the same patterns as system-admin-index.html.twig
#}
{{ attach_library(active_theme()~'/admin.config') }}

{# do we really want this ?? #}
{{ system_compact_link }}

{% include active_theme_path() ~ '/templates/component/navigation/cardlayout/cardlayout.html.twig' %}
<section{{ attributes.addClass('L','LW-4', 'LW--nooverflow', 'pagewidth', 'js-card-layout-target') }}>

  {% for container in containers %}

      {% for block in container.blocks %}
        {# layout-landing/config/admin-block.html.twig #}
        {{ block }}
      {% endfor %}


  {% endfor %}
</section>
