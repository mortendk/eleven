{#
we need this template so we dont messes with views admin ui specific elements
 #}
{{ attach_library('eleven/button') }}
{{ attach_library('eleven/form.form') }}

{%
  set remove_classes = [
  'text-full',
  'form-text',
  ]
%}

{% set classes = [


]
%}

<input
{{ attributes.
  removeClass(remove_classes).
  addClass(classes).
  removeAttribute('size')
}}
/>{{ children }}
