  <h2>{{ 'General System Information'|t }}</h2>

  <section class="cards">
    {# Drupal version #}
    <article class="card">
      <div class="card__icon card__icon--drupal"></div>
      <div class="">
        <h3>{{ 'Drupal Version'|t }}</h3>
        <div class="card__text card__text--small">
          {{ drupal.value }}
          {{ drupal.description }}
        </div>
      </div>
    </article>

    {# php #}
    <article class="card">
      <div class="card__icon card__icon--php"></div>
      <div class=''>
        <h3>{{ 'PHP'|t }}</h3>
        <div class="card__text card__text--small">
          <strong>{{ 'Version'|t }}</strong>
          {{ php.value }}
          {% if php.description %}

          <br>
            {{ php.description }}
          {% endif %}

          <br>
          <strong>{{ 'Memory limit'|t }}</strong>
          {{ php_memory_limit.value }}
          {{ php_memory_limit.description }}
        </div>
      </div>
    </article>

    {# webserver #}
    <article class="card">
      <div class="card__icon card__icon--webserver"></div>
      <div class="">
        <h3>{{ 'Web Server'|t }}</h3>
        <div class="card__text card__text--small">
        {{ webserver.value }}
        {% if webserver.description %}
          {{ webserver.description }}
        {% endif %}
        </div>
      </div>
    </article>

    {# DB #}
    <article class="card">
      <div class="card__icon card__icon--database"></div>
      <div class="">
        <h3>{{ 'Database'|t }}</h3>
        <div class="card__text card__text--small">
        {{ 'Version'|t }}
        {{ database_system_version.value }}
        <br>
        {% if database_system_version.description %}
          {{ database_system_version.description }}
        {% endif %}

        <strong>{{ 'System'|t }}</strong><br>
        {{ database_system.value }}
        {% if database_system.description %}
          {{ database_system.description }}
        {% endif %}
        </div>
      </div>
    </article>

    {# cron #}
    <article class="card">
      <div class="card__icon card__icon--cron"></div>
      <div class="">
        <h3>{{ 'Last Cron Run'|t }}</h3>
        <div class="card__text card__text--small">
        {{ cron.value }}
        <br>
        {% if cron.run_cron %}
          {{ cron.run_cron }}
        {% endif %}
        <br>
        {% if cron.description %}
          {{ cron.description }}
        {% endif %}
      </div>
      </div>
    </article>


</section>
