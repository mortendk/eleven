{{ attach_library(active_theme()~'/form.form') }}
{{ attach_library(active_theme()~'/form.file') }}


<input
{{ attributes.
  removeClass(remove_classes).
  removeAttribute('size')
}}
/>{{ children }}
  <label for="{{ element['#id']}}" ><span>Choose a file</span></label>
