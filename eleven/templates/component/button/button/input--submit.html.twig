{{ attach_library(active_theme()~'/button') }}
{{ attach_library(active_theme()~'/form.form') }}



{% set classes = [

]
%}

<span class="form-item--input">

  <input
  {{ attributes.
    removeClass(remove_classes).
    addClass(classes).
    removeAttribute('size')
  }}
  />{{ children }}

  <div class="form-item__icon form-item__icon--{{ input_type}}"></div>
</span>
