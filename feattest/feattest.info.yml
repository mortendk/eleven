name: Elven FEAT test
type: theme
description: 'theme made for testing FEAT eleven theme'
base theme: elevenfeat # grap the eleven feat theme elements.

core: '8.x'

# Test that this work even if we remove all the libraries
# libraries-override:
#   system/base: false


# we have to set the ckeditor default in the frontend facing theme.
# so it can be picked up in the backend as well
ckeditor_stylesheets:
  - ckeditoriframe.css
